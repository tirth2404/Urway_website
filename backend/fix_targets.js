const mongoose = require('mongoose'); mongoose.connect('mongodb+srv://tirth2404:tirth2404@cluster0.qut1y8v.mongodb.net/urway').then(async () => { const db = mongoose.connection.db; const targets = await db.collection('roadmap_targets').find({ keywordsRaw: '' }).toArray(); for (const t of targets) { try { const res = await fetch('http://127.0.0.1:5007/api/keywords/predict', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ description: t.description }) }); const data = await res.json(); await db.collection('roadmap_targets').updateOne({ _id: t._id }, { $set: { keywordsRaw: data.keywords_raw, keywordsList: data.keywords_list } }); console.log('Updated:', t.targetName); } catch (e) { console.error('Failed for:', t.targetName, e.message); } } console.log('Done!'); process.exit(0); });
